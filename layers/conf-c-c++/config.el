;;; config.el --- Additional C/C++ Layer for Spacemacs -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(defvar lsp-remap-xref-keybindings t "When non-nil, xref keybindings remapped to lsp-ui-peek-find-*")
(defvar c-c++-default-mode-for-headers 'c-mode
  "Default mode to open header files. Can be `c-mode' or `c++-mode'.")
